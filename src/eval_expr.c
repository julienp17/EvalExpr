/*
** EPITECH PROJECT, 2019
** C Pool EvalExpr
** File description:
** Returns the result of an expression as an integer
*/

int eval_expr(char const *str)
{
    if (str == 0)
        return (1);
    return (0);
}
